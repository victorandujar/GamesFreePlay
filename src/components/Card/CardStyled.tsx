import styled from "styled-components";

const CardStyled = styled.article`
  color: black;

  .card-title {
    font-weight: 800;
  }

  .btn {
    color: #fff;
    cursor: pointer;
  }
`;

export default CardStyled;
