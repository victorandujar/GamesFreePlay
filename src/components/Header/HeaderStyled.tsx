import styled from "styled-components";

const HeaderStyled = styled.header`
  display: flex;
  align-items: center;
  gap: 10px;

  .main-header {
    &__title {
      color: #ffe600;
      font-size: 28px;
    }
  }
`;

export default HeaderStyled;
